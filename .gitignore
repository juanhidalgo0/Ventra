node_modules
dist
*.db
*.db-journal
.env
.DS_Store
*.rar
*.exe
*.zip
firebase-credentials.json
*credentials.json
*.pem
*.key

# Secretos y datos locales: nunca a git
PASSWORDS.txt
updaterPASSWORD.txt
github-recovery-codes.txt
*.db-wal
*.db-shm
*.before-restore-*
apps/backend/backups/
*.secret
.backups/

# Estado local de suscripción y sincronización (contiene el secreto de la PC)
ventra-subscription.json
ventra-sync.json

# Copias movidas fuera del build (no van al repo)
/backups-recuperados
